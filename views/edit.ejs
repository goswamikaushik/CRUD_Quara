<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit post</title>
</head>
<body>
    <div class="container">
        <h2> Edit your post</h2>
        <% if (post) { %>
            <p> post id : <%= post.id %></p>
            <div class="post">
                <h3> @<%= post.username %></h3>
                
            </div>
         
        <form method="post" action="/posts/<%=post.id%>?_method=PATCH">
            <textarea name="content" id="" cols="40" rows="20"><%= post.content%></textarea><br>

            <button>Submit</button>
        </form>
        <% } else { %>
            <p>Post not found</p>
            <% } %>
    </div>
</body>

</html>